var chai = require('chai'),
  assert = chai.assert,
  expect = chai.expect;

describe('Timeline', function() {

  var timeline = require('../js/timeline');

  it('is running', function() {
      assert.equal(true, true);
  });

});
